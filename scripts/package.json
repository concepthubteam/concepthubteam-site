{
  "name": "gozi-cron",
  "version": "1.0.0",
  "description": "GOZI Cron Server — heat updates + cleanup",
  "main": "cron.js",
  "scripts": {
    "start": "node cron.js",
    "dev":   "node --watch cron.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
